language: java
jdk: openjdk8

env:
  - COVERAGE=0.055 TARGET=./monolithic/ui
  - COVERAGE=0.145 TARGET=./monolithic/service/cart
  - COVERAGE=0 TARGET=./monolithic/service/user
  - COVERAGE=0.3 TARGET=./monolithic/repository/order
  - COVERAGE=0 TARGET=./monolithic/repository/cart
  - COVERAGE=0.125 TARGET=./monolithic/repository/product
  - COVERAGE=0.3 TARGET=./monolithic/repository/user
  
install:
  - ./gradlew clean jar

after_success:
  - ./gradlew -p $TARGET check

deploy:
  provider: script
  script: echo 'WIP'
  on:
    all_branches: true
